import 'package:flutter/material.dart';

class ClubsScreen extends StatelessWidget {
  static const routeName = '/clubls-screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Clubls"),
      ),
    );
  }
}
